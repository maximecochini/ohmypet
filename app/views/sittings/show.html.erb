<div class="container">
  <h2><%= "#{@pet.name}'s sitting" %></h2>
  <br>
  <ul>
    <li><%= "From #{sitting.start_date} to #{sitting.end_date}"%></li>
    <li><%=  "You will make #{pet.reward_per_day} per day !" %></li>
    <li><%=  " Pick up #{@pet.name} at #{pet.street_address}, #{pet.city}, #{pet.postcode}, #{pet.country}"%></li>
  </ul>
  <%= link_to "back to dashboard", dashboard_path, class: "btn btn-primary"  %>
</div>
